use sakila;
show tables;

-- insert into posts(id, name, slug, createdAt, updatedAt, deletedAt)
-- values(1,'first post name','first-post-name', current_timestamp(), current_timestamp(), null);

-- select * from posts;
-- RESET users
-- truncate table users;
select * from users;
select * from SequelizeMeta;